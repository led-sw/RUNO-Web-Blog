const swiper = new Swiper('.swiper', {
    // Optional parameters

    // If we need pagination
    pagination: {
        el: '.slider__pagination',
    },
    autoplay: {
        delay: 5000,
    },
});
